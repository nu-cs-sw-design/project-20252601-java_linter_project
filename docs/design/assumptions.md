Assumptions made:
- The user has access to an OpenAI API key for full functionality.
- The user either will enter the fully qualified name for classes in current project (test bad code) or give path to compiled .class files 
- The uer has everything installed as used in class so far
- The user has access to internet (for LLM calls)